apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-cd-backend-pvc
  namespace: ci-cd-dashboard
  labels:
    app: ci-cd-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
