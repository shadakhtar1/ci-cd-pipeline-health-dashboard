apiVersion: v1
kind: ConfigMap
metadata:
  name: ci-cd-backend-config
  namespace: ci-cd-dashboard
  labels:
    app: ci-cd-backend
    component: backend
data:
  DATABASE_URL: "sqlite:///./data/app.db"
  APP_NAME: "ci-cd-dashboard"
  ENVIRONMENT: "minikube"
